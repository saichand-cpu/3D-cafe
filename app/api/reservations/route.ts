import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';
const email=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;
function db(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;return url&&key?createClient(url,key,{auth:{persistSession:false}}):null}
export async function POST(request:Request){try{const body=await request.json();const name=String(body.name??'').trim();const guestEmail=String(body.email??'').trim();const phone=String(body.phone??'').trim();const date=String(body.date??'').trim();const time=String(body.time??'').trim();const guests=Number(body.guests??2);const notes=String(body.notes??'').trim();if(!name||!email.test(guestEmail)||!phone||!date||!time||!Number.isInteger(guests)||guests<1||guests>20)return NextResponse.json({error:'Please complete all required fields.'},{status:400});const client=db();if(client){const {error}=await client.from('reservations').insert({name,email:guestEmail,phone,date,time,guests,notes,status:'pending'});if(error)throw error}return NextResponse.json({ok:true,mode:client?'database':'demo'})}catch{return NextResponse.json({error:'Unable to create reservation. Please try again.'},{status:500})}}
export async function GET(request:Request){const key=request.headers.get('x-admin-key');if(!key||key!==process.env.ADMIN_DASHBOARD_KEY)return NextResponse.json({error:'Unauthorized'},{status:401});const client=db();if(!client)return NextResponse.json({reservations:[],mode:'demo'});const {data,error}=await client.from('reservations').select('*').order('created_at',{ascending:false}).limit(100);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({reservations:data,mode:'database'})}
